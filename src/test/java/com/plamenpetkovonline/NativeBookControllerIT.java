package com.plamenpetkovonline;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeBookControllerIT {

    // Execute the same tests but in native mode.
    // Need to extend those tests.
}